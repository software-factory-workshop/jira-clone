# Work already underway

Updated 13 September 2026 after the Jira demo contract review. Verify live GitHub state before treating this dated record as current.

## Current assignment: build Jira through the factory

The development session implements factory capabilities; native Eve workers implement Jira. The accepted [Jira teaching loop](../tasks/jira-teaching-loop.md) is now merged on `main`: seeded issues, editable priority, assignee filtering, a bounded save path, deterministic save-failure verification, reset, comments and a demo transition guard. See [the current Jira contract](../../docs/jira-current-state.md). Do not rediscover this slice as an unassigned proposal; extend it only with a new scenario.

The factory work adds API control for cockpit capabilities and a reusable delivery loop: worker PR, exact-head independent review, original-owner revision and fresh review. Missing browser evidence stops for human review. Polling the delivery API drives progression; Eve owns durable model execution. No station merges PRs or activates factory policy. Changes to policy are separately reviewed and merged by the development session under Remi's authorization.

## Integrated work

PRs #2 through #12 are merged. These cover proposal feedback, worker review context, removal of model budgets, host-owned review blockers, state-record reconciliation, CI packaging authentication, cockpit revision controls, branch ownership and revisions, feedback recovery and the Jira teaching contract. PR #1 is closed unmerged; its earlier cockpit API draft is historical, not the current API implementation.

The factory has native task mining, per-finding task drafts, a worker that publishes a draft PR and an independent reviewer. Same-owner revisions and child PRs were exercised against real GitHub branches. An old reviewer accepted incomplete evidence; PR #5 fixed the host policy. Earlier approval and deployment claims must be read with their dated limitations in `docs/verification.md` and `factory/branch-ownership-proof.md`.

## Existing context and identity capabilities

Native task-mining baseline and transfer experiments already exist; do not propose rebuilding them without a distinct hypothesis. fx is a separate experiment, never the production agent harness. Shared machine-to-machine Vercel access uses the app's Connect credential. A former per-user access gap was resolved; runtime-log coverage and task usefulness must still be judged from actual run evidence.

GitHub and Vercel inventories, source snapshots, reflection documents and stored proposals supply context. Synthetic fixtures and browser-local drafts are not production persistence. The Jira demo's persistence boundary is established and labelled in [the current Jira contract](../../docs/jira-current-state.md); hosted browser evidence still needs to be recorded separately.
