// Each root mounts independently and gets a sandbox-derived browser session.
export const browserOptions={contentBoundaries:true,inlineScreenshots:true,maxOutputChars:30000,installSpec:'agent-browser@0.37.1'};
