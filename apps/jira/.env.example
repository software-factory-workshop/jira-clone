# Neon Postgres connection string for the Jira issue/comment store.
DATABASE_URL=postgresql://user:password@host/database?sslmode=require

# Optional. Defaults to Neon when DATABASE_URL is present, otherwise memory.
# JIRA_PERSISTENCE=neon
